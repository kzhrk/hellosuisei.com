box: node:latest
build:
  steps:
    - npm-install
    - script:
        name: build static files
        code: npm run build
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: hellosuisei.com
        basedir: dest
