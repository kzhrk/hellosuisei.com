box: node:latest
build:
  steps:
    - npm-install
    - grunt:
        tasks: production
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: hellosuisei.com
        basedir: dest
